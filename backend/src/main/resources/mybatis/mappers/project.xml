<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="project">	
		<select id="findAll" parameterType="long" resultType="projectvo">
				<![CDATA[
				select no, project_name as projectName, start_date as start, end_date as end, descrition as description, status from project where no= 1;

				]]>
		</select>
		<insert id="insert" parameterType="projectvo">
				<![CDATA[
				insert into task value(null, 1, #{userNo}, #{title}, #{start}, #{end}, "Todo", 1);
				]]>
		</insert>
		
		<update id="update" parameterType="projectvo">
				<![CDATA[
				update task set user_no=#{userNo}, task_name=#{title}, start_date=#{start}, end_date=#{end}, status="Todo" where no=#{id};
				]]>
		</update>
		
		<delete id="delete" parameterType="long">
				<![CDATA[
				delete from task where no=#{id};				
				]]>
		</delete>

</mapper>
