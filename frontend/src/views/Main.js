import React, { useCallback, useEffect } from 'react';
import SiteLayout from "../components/layout/SiteLayout";

export default function Main() {
    // dispatch - setfocus 데이터 전역으로 넣고


    return (
        <SiteLayout>
            크루를 만들어주세요!!!
        </SiteLayout>
    );
}